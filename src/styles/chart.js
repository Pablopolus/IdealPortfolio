import { StyleSheet } from 'react-native'

export const chartStyles = StyleSheet.create({
    legendContainer: {        
        flexDirection: 'row',
        paddingBottom: 10
    },
    legendBackground: {
        width: 20,
        height: 20,        
        marginRight: 5
    }
})
